# Email Configuration
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-gmail-app-password
ADMIN_EMAIL=admin@vishnorex.com

# Custom SMTP Configuration (optional - for non-Gmail providers)
# SMTP_HOST=smtp.your-provider.com
# SMTP_PORT=587
# SMTP_SECURE=false

# Server Configuration
PORT=3000

# Security Configuration
ALLOWED_ORIGINS=*

# Instructions:
# 1. Copy this file to .env
# 2. Replace 'your-gmail-app-password' with your actual Gmail App Password
# 3. To generate Gmail App Password:
#    - Go to Google Account settings
#    - Enable 2-Factor Authentication
#    - Go to Security > App passwords
#    - Generate a new app password for this application
#    - Use that 16-character password here (without spaces)
# 4. For production, set ALLOWED_ORIGINS to your domain (e.g., https://yourdomain.com)
# 5. For custom SMTP providers, uncomment and configure the SMTP_* variables